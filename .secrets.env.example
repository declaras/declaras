# Credenciales locales para calibrar el conector contra el portal real.
# Este archivo NO se versiona (esta en .gitignore) y NO debe compartirse por chat.
#
# Para escribirlo sin que la clave quede en el historial de la terminal:
#   read -s -p "clave: " p && printf 'DIAN_CC=%s\nDIAN_PASSWORD=%s\n' "TU_CEDULA" "$p" > .secrets.env && unset p
DIAN_CC=
DIAN_PASSWORD=
